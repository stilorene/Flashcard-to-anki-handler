# This document exports the flashcards to the selected Anki deck using the AnkiConnect API.


def export_cards_to_deck(selected_deck):
    # Hier kommt die Logik hin, um Karten in den ausgewählten Stapel zu importieren
    print(f"Importiere Karten in den Stapel: {selected_deck}")
    # Beispiel: Aufruf einer Funktion, die Karten importiert
    import_cards(selected_deck)


